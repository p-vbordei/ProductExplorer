/your_project_directory
├── main.py
└── templates
    └── index.html


# In the Working Dir
docker build -t flask-gae-app .
docker run -p 8080:8080 flask-gae-app

# Then, access the app in a browser at http://localhost:8080/.



# pip install aiohttp numpy pandas tenacity requests tiktoken nest_asyncio requests google-cloud-firestore firebase_admin tqdm python-dotenv flask gunicorn "connexion[swagger-ui]"

# pip install firebase_admin google-cloud-firestore nest_asyncio tiktoken "connexion[swagger-ui]"
# conda install aiohttp numpy pandas tenacity requests requests tqdm python-dotenv flask gunicorn

export FLASK_APP=main.py
export FLASK_ENV=development
flask run




flask_app_test/
│
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── products_processing.py
│   ├── other_module1.py
│   ├── other_module2.py
│   └── ...
│
├── templates/  (if you have any HTML templates for Flask)
│   ├── ...
│
├── static/  (if you have any static files like CSS, JS, images)
│   ├── ...
│
├── Dockerfile
├── requirements.txt
├── app.yaml
└── swagger.yaml
Here's a breakdown:

app/: This is the main application directory where all your Python code resides.

__init__.py: This is an empty file that makes the app directory a Python package, allowing you to import modules from it.
main.py: This is your main Flask application file.
products_processing.py: This is the file you mentioned that contains the run_products_investigation function.
other_module1.py, other_module2.py, etc.: Any other Python modules you might have.
templates/: If you're using Flask's templating engine, this is where your HTML templates would go.

static/: If you have static files (like CSS, JS, images), they would go here.

Dockerfile: This is the Dockerfile you provided.

requirements.txt: This file contains all the Python package dependencies for your project. Make sure to include all the necessary packages, including those required by products_processing.py.

app.yaml: This is the configuration file for deploying to Google App Engine.

swagger.yaml: This is the Swagger API definition file.